#pragma once

namespace pztrainer::ui {

bool DrawAnimationDropdown(int* selected_animation, bool player_preview = false,
                           bool animal_preview = false);

}  // namespace pztrainer::ui
